// Assign the configuration object to a named variable
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};

// Then export the variable as the default
export default config;
